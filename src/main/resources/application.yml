spring:
  application:
    name: opendata-crud
  datasource:
    url: jdbc:mariadb://localhost:3307/polytech
    username: root
    password: password
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect